# Copy this file to keystore.properties and replace each placeholder locally.
# Never commit the populated keystore.properties file or the keystore itself.
storeFile=path/to/release-keystore.jks
storePassword=REPLACE_LOCALLY
keyAlias=release
keyPassword=REPLACE_LOCALLY
